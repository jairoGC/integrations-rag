# Ralph Progress Log
Started: Sat Jan 24 15:22:50 -05 2026
---

## 2026-01-24 17:00 - US-001
- Implemented PDF document ingestion pipeline
- Created PDFIngestionPipeline class with text extraction and metadata parsing
- Handles document types: provider_doc and jira_ticket
- Extracts provider name and date from filename conventions
- Graceful error handling for malformed PDFs
- Files changed:
  - src/ingestion/pdf_ingestion.py (new)
  - src/ingestion/__init__.py (new)
  - tests/test_pdf_ingestion.py (new)
  - requirements.txt (new)
  - pyproject.toml (new)
- **Learnings:**
  - This project uses Python 3.10 with type annotations
  - PyPDF2 library for PDF text extraction
  - Filename format: {document_type}_{provider_name}_{date}.pdf
  - Document types must match exactly: "provider_doc" or "jira_ticket"
  - Quality checks required: mypy (strict mode) and pylint (9.0+ score acceptable)
  - All tests must pass before committing
---

## 2026-01-24 17:30 - US-002
- Implemented text chunking strategy with recursive character splitter
- Created TextChunker class with configurable chunk size and overlap
- Default: 1000 tokens (4000 chars) with 200 token overlap (800 chars)
- Section header extraction supporting markdown (#), all-caps, and colon-ending formats
- Chunks inherit all parent document metadata (document_type, provider_name, etc.)
- Added section_title to chunk metadata when headers detected
- Chunk statistics: count, avg/min/max size, size distribution by bucket
- Files changed:
  - src/chunking/text_chunker.py (new)
  - src/chunking/__init__.py (new)
  - tests/test_text_chunker.py (new, 16 tests)
- **Learnings:**
  - Use dataclasses for clean data structures (Chunk class)
  - Recursive splitting with multiple separators (paragraph, sentence, word)
  - Character-to-token ratio: approximately 4 chars per token
  - Overlap helps maintain context between chunks
  - Section headers provide important chunk context
  - Small chunks should be merged to avoid very short chunks
---
## 2026-01-26 - US-003
- Implemented vector database setup and indexing with MongoDB Atlas
- Created VectorStore class for database operations and chunk indexing
- Created EmbeddingProvider class for OpenAI text-embedding-3-small embeddings
- Embedding dimensions: 1536 (OpenAI text-embedding-3-small)
- Automatic metadata index creation on document_type and provider_name
- Batch upsert operations supporting configurable batch sizes (default: 100)
- Files changed:
  - src/indexing/__init__.py (new)
  - src/indexing/vector_store.py (new)
  - tests/test_vector_store.py (new, 23 tests)
  - requirements.txt (updated for dependency compatibility)
  - README.md (updated with vector indexing documentation)
- **Learnings for future iterations:**
  - Environment variables required: OPENAI_API_KEY and MONGODB_URI
  - OpenAI embedding API supports batch operations for efficiency
  - MongoDB upsert pattern: update_one with upsert=True for single, bulk_write for batches
  - Use UpdateOne operations in bulk_write for efficient batch upserts
  - Metadata indexes should be created at initialization for query performance
  - Mock external dependencies (OpenAI, MongoDB) in tests for fast, reliable testing
  - Requirements.txt version pinning: use >= for flexibility with langchain dependencies
  - VectorStore.index_batch processes chunks in configurable batches to avoid API limits
  - Embedding generation should filter out empty texts to avoid API errors
---
## 2026-01-26 - US-004
- Implemented metadata-filtered retrieval system with query classification
- Created QueryClassifier for extracting provider names and document types from queries
- Supports 10+ payment providers: Stripe, PayPal, Adyen, Braintree, Square, etc.
- Created Retriever class with cosine similarity search using numpy
- Automatic metadata filtering based on query classification results
- Fallback mechanism: tries filtered search first, falls back to unfiltered
- Top-k retrieval with configurable parameters (default k=5)
- Minimum similarity threshold support for filtering low-quality results
- Files changed:
  - src/retrieval/__init__.py (new)
  - src/retrieval/retriever.py (new)
  - tests/test_retriever.py (new, 36 tests)
  - requirements.txt (added numpy for cosine similarity)
  - README.md (updated with retrieval examples)
- **Learnings for future iterations:**
  - Cosine similarity formula: dot(a,b) / (norm(a) * norm(b))
  - Numpy provides efficient vector operations for similarity computation
  - Regex word boundaries (\b) prevent false provider matches (e.g., "striped" vs "stripe")
  - Query classification should happen before retrieval to enable metadata filtering
  - MongoDB $in operator supports multiple provider filters in single query
  - Mock objects need proper __iter__ implementation for list() compatibility in tests
  - Retriever uses get_chunks_by_metadata when filters detected, collection.find() otherwise
  - QueryClassifier is case-insensitive but returns capitalized provider names
  - Fallback pattern improves user experience when filters are too restrictive
---
## 2026-01-26 - US-005
- Implemented LLM-based answer generation with Claude and GPT-4 support
- Created AnswerGenerator class with unified interface for both providers
- System prompt enforces context-only answers with source citations
- Automatic detection of insufficient information responses
- Context formatting includes source metadata (filename, provider, type)
- Citation formatting for user-friendly display
- Support for Claude models: claude-3-opus, claude-3-sonnet
- Support for GPT models: gpt-4, gpt-3.5-turbo
- Configurable minimum chunks threshold before generating answer
- Files changed:
  - src/generation/__init__.py (new)
  - src/generation/answer_generator.py (new)
  - tests/test_answer_generator.py (new, 21 tests)
  - requirements.txt (added anthropic>=0.18.0)
  - README.md (updated with generation examples)
- **Learnings for future iterations:**
  - OpenAI and Anthropic have different API structures (chat.completions vs messages)
  - Claude returns content blocks, GPT returns message.content directly
  - Use Union types for clients that can be different types
  - Type: ignore comments needed when mocking prevents isinstance checks
  - System prompts should explicitly request source citations
  - Answer detection: check for phrases like "don't have enough information"
  - Claude API requires system parameter separate from messages array
  - GPT API includes system message in messages array as role "system"
  - Temperature=0.7 works well for balanced creativity and accuracy
  - Max tokens 1024 sufficient for most documentation answers
---
## 2026-01-26 - US-006
- Implemented complete CLI query interface for RAG pipeline
- Created RAGPipeline class that integrates all components (ingestion, retrieval, generation)
- Command-line argument parsing with argparse
- Rich formatted output with sections: query, filters, answer, sources, stats
- Displays metadata filters applied (providers, document types)
- Performance timing breakdown (retrieval, generation, total time)
- Environment variable validation (MONGODB_URI, API keys)
- Support for both GPT and Claude models via CLI flags
- Configurable parameters: --top-k, --min-similarity, --model, --database, --collection
- Graceful error handling with user-friendly messages
- Warning when response exceeds 3 second target
- Files changed:
  - src/cli.py (new)
  - tests/test_cli.py (new, 20 tests)
  - README.md (updated with CLI usage examples)
- **Learnings for future iterations:**
  - argparse provides clean CLI interface with help text
  - nargs="?" allows positional argument to be optional
  - Environment variable validation should happen before initialization
  - Use logging for internal messages, print() for user output
  - Format response separately from generation for flexibility
  - Time each stage separately for performance debugging
  - sys.exit() return codes: 0=success, 1=error, 130=interrupted
  - Include usage examples in argparse epilog for better UX
  - Validate API key based on model choice (Claude vs GPT)
  - Pipeline integration: VectorStore -> Retriever -> AnswerGenerator
  - Use retrieve_with_fallback for better user experience
---
## 2026-01-26 - US-007
- Implemented precision@k evaluation metrics for RAG retrieval
- Created PrecisionEvaluator class that calculates precision = (relevant in top-k) / k
- Also implemented RecallEvaluator and LatencyTracker in same module (will be used for US-008 and US-009)
- EvaluationResult dataclass for standardized result storage
- format_evaluation_report function for human-readable output
- Configurable k values, target thresholds, and per-query tracking
- Files changed:
  - src/evaluation/__init__.py (new)
  - src/evaluation/metrics.py (new)
  - tests/test_evaluation.py (new, 30 tests)
- **Learnings for future iterations:**
  - Precision@k formula: divide by k (parameter), not by len(retrieved) for consistent scoring
  - Test functions that execute too quickly may return latency=0.0, use >= 0 not > 0 assertions
  - RecallEvaluator returns dict mapping k values to EvaluationResult objects
  - LatencyTracker.evaluate() resets latencies automatically to avoid contamination
  - Optional type hints required: use Optional[List[int]] not List[int] = None
  - Percentile calculation needs interpolation between data points for accuracy
  - EvaluationResult.to_dict() allows easy serialization to JSON for reporting
  - Target thresholds checked automatically, results include target_met boolean
---

## 2026-01-26 - US-008 & US-009
- US-008 (Recall evaluation) and US-009 (Latency tracking) were implemented together with US-007
- RecallEvaluator class calculates recall@k for multiple k values (default: 5, 10, 20)
- Returns dict mapping k to EvaluationResult, checks target for k=10 (>85% target)
- LatencyTracker class measures query latency and calculates p50, p95, p99 percentiles
- Custom percentile calculation with linear interpolation for accuracy
- LatencyTracker.evaluate() measures end-to-end pipeline latency, checks p95 < 3s target
- Both classes tested (8 tests for Recall, 7 for Latency) and share same quality metrics
- Files changed:
  - src/evaluation/metrics.py (RecallEvaluator and LatencyTracker already included)
  - tests/test_evaluation.py (tests already included)
- **Learnings for future iterations:**
  - Related evaluation metrics should be grouped in single module for cohesion
  - RecallEvaluator.evaluate() returns Dict[int, EvaluationResult] not single result
  - LatencyTracker stores all latencies for percentile calculation across test set
  - measure_query() is reusable for any callable, not just pipeline queries
  - percentile() function uses linear interpolation: (f*(c-k) + c*(k-f))
  - Evaluation modules naturally group together: precision, recall, latency all in metrics.py
---

